{
  "name": "goudalle-sync-agent",
  "version": "1.0.0",
  "description": "Agent de synchronisation des dossiers chantier vers le serveur intranet Goudalle",
  "main": "sync.js",
  "scripts": {
    "start": "node sync.js",
    "install-windows": "schtasks /create /tn \"GoudalleSync\" /tr \"node \\\"%~dp0sync.js\\\"\" /sc onlogon /f"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
